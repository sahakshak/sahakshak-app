import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textInputStyle: {
    borderRadius: 50,
    margin: 4,
    marginStart: 12,
    marginEnd: 12,
    paddingHorizontal: 8,
    fontSize: 16,
  },
  outlineStyle: {
    borderRadius: 50,
  },
});

export default styles;
